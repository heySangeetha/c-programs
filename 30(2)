#include<stdio.h>
#include<string.h>
void main()
{
	char str[1000];
	int i;
	printf("Enter the string:");
	fgets(str,1000,stdin);
	for(i=0;i<strlen(str);i++)
	{
		if(str[i]=='a')
		{
			printf("A");
		}
		else
		{
			printf("%c",str[i]);
		}
	}
}
